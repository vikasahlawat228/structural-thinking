# <Project Name> — Context

> Template — copy to the root of any project that uses these skills, replace
> the placeholders, and update it inline as decisions are made during
> `grill-with-docs` sessions.

One paragraph describing what this project does, in the team's own words.

## Glossary

| Term     | Definition                                          | Related        |
| -------- | --------------------------------------------------- | -------------- |
| `<term>` | One- or two-line definition. Domain-meaningful only.| `<term>`, `<term>` |
| `<term>` | …                                                   | …              |

## Domain rules

Hard rules that must hold across the system. One bullet each.

- `<rule>`
- `<rule>`

## Anti-glossary

Terms we do **not** use, and what to say instead.

| Don't say  | Say instead     |
| ---------- | --------------- |
| `<term>`   | `<better term>` |

## Modules / bounded contexts

If the project has multiple bounded contexts, list them and link the
per-module `CONTEXT.md`. Otherwise delete this section.

- **`<module>`** — `<one-line responsibility>` — see `src/<module>/CONTEXT.md`
- **`<module>`** — `<one-line responsibility>` — see `src/<module>/CONTEXT.md`
